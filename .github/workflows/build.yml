name: Legacy release pipeline disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the release block
        run: |
          echo "Data Viewer v1 release is intentionally blocked."
          echo "Use the Data Viewer quality workflow for Windows and Ubuntu verification."
          echo "A release pipeline will be added only after the v1 package, artifact smoke tests, and PyQt licensing decision are complete."
          exit 1
